version: '3.4'

services:
  frontend:
    image: ${DOCKER_REGISTRY-}frontend
    build:
      context: .
      dockerfile: Dockerfile
    ports:
    - "8080:8080"
networks:
  default:
    external:
      name: eshoponweb_default
